Place `narration.wav` or `narration.mp3` here to use a manual voice track.

If this folder is empty and the storyboard enables voiceover, the pipeline runs local Piper TTS into `build/audio/voice/` instead. See `docs/tts.md`.
